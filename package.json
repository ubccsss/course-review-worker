{
  "private": true,
  "name": "github-issues-worker",
  "version": "1.0.0",
  "description": "A Cloudflare worker for creating GitHub issues for new course reviews for ubccsss.org's courses database.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "format": "prettier --write '**/*.{js,css,json,md}'"
  },
  "devDependencies": {
    "@cloudflare/wrangler": "^1.19.7",
    "prettier": "^1.18.2"
  },
  "dependencies": {
    "@octokit/rest": "^18.12.0"
  }
}